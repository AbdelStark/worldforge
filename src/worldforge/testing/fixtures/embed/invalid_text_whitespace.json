{
  "schema_version": 1,
  "id": "embed.invalid_text_whitespace",
  "capability": "embed",
  "data_class": "synthetic",
  "expected": "invalid",
  "expected_error_pattern": "whitespace",
  "description": "Whitespace-only text is invalid for embed: it carries no signal to embed, and the facade rejects the request before provider dispatch.",
  "payload": {
    "text": "   "
  }
}
