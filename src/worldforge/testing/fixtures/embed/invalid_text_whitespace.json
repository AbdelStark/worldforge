{
  "schema_version": 1,
  "id": "embed.invalid_text_whitespace",
  "capability": "embed",
  "data_class": "synthetic",
  "expected": "invalid",
  "expected_error_pattern": "whitespace",
  "description": "Whitespace-only text is documented as invalid for embed: it carries no signal to embed and providers should reject it. The framework does not reject this today; the fixture pins the contract for future input validation.",
  "payload": {
    "text": "   "
  }
}
